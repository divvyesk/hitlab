import { LoginForm } from "@/components/auth/LoginForm";

export const metadata = {
  title: "Sign In — HitLab AI",
  description: "Sign in to your HitLab AI account",
};

export default function LoginPage() {
  return <LoginForm />;
}
