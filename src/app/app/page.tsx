import { AppDashboard } from "@/components/AppDashboard";

export const metadata = {
  title: "Studio — HitLab AI",
  description: "Predict Billboard hits with AI-powered music analysis",
};

export default function AppPage() {
  return <AppDashboard />;
}
